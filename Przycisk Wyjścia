import pygame
import sys
from pygame.locals import *

#Przykładowy przycisk wyjścia, z przykładowymi wymiarami i nazwami. Na jego podstawie można też znakomicie robić inne przyciski.

screen = pygame.display.set_mode((400, 400))
clickme = pygame.Surface((60, 40))
detecto = pygame.Rect(clickme.get_rect())
clickme.fill((10, 100, 20))
screen.blit(clickme, (170, 180))

while True:
    for event in pygame.event.get():
    if event.type == MOUSEBUTTONDOWN:
            x, y = event.pos
            if 170 < x < 230 and 180 < y < 220:
            if detecto.rcollidepoint(pos):
              sys.exit
            
